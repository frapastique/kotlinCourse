/*
class Person {
  var age: Int

  constructor(age: Int) {
    this.age = age
  }

  fun birthday(){
    age = age + 1
  }

  fun isUnderage() : Boolean {
    return(age < 18)
  }
}

Hier wird die Klasse "Person" mit der Variable "age" als Integer angelegt.
Zudem besitzt diese die Methoden "birthday" und "isUnderage".

Im Konstruktor wird der Klassen Variable "age" übergeben.

In der Methode "birthday" wird die Eigenschaft age um 1 erhöht.

In der Methode "isUnderage" wird, wenn die Eigenschaft "age" unter 18 ist
der Boolean true zurückgegeben.

var person = Person(15)
person.birthday()
println(person.isUnderage())

Innerhalb diesen Codeabschnitts wird die Variable "person" in der Klasse "Person"
mit der Eigenschaft "age" 15 initialisiert.

Darauf wird auf "person" die Methode "birthday" angewendet, welche die Eigenschaft
"age" um 1 erhöht.

Nun wird auf "person" die Methode "isUnderage" angewendet, welche "true" in der Konsole
ausgibt. (Da die "person" unter 18 ist)
 */